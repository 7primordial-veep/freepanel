PANEL_BASE=https://your-panel.example.com:8443
PANEL_USER=cmsadmin
PANEL_PASS=replace-with-your-rotated-password

# Dedicated admin test user (kept separate from cmsadmin so the cmsadmin session
# isn't polluted by automated tests). Used by tools/panel-double-spot.mjs.
PWTEST_USER=pwtest
PWTEST_PASS=replace-with-your-pwtest-password
