{% extends 'Frontend/layout.html.twig' %}

{% block pageTitle %}{% trans %}Import Certificate{% endtrans %}{% endblock %}

{%- block stylesheets -%}
  {{ parent() }}
  <link href="{{ asset('/assets/css/frontend/site.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}
<div class="container-fluid container-limited-width">
  <div class="site-container">
    {{ include('Frontend/Site/Partial/site-info.html.twig', {'site': site}) }}
    <div class="site-content-container">
      {{ include('Frontend/Site/Partial/tab-container.html.twig', {'active': 'certificates', 'site': site}) }}
      {{ include('Partial/flash-messages.html.twig', { 'flashBagMessages': app.session.flashbag.all }) }}
      {{ include('Partial/form-errors.html.twig', { 'formErrors': formErrors }) }}
      <div class="site-content">
        {{ form_start(form) }}
        <div class="card card-table">
          <div class="card-header d-flex justify-content-between">
            {% trans %}Import Certificate{% endtrans %}
          </div>
          <div class="card-body card-body-no-padding">
            <div class="card-form">
              <div class="row">
                <div class="col-12 {{ (form_errors(form.privateKey) ? 'has-error' : '') }}">
                  {{ form_label(form.privateKey, null, {'label_attr': {'class': 'col-form-label'}}) }}
                  {{ form_widget(form.privateKey) }}
                </div>
              </div>
              <div class="row">
                <div class="col-12 {{ (form_errors(form.certificate) ? 'has-error' : '') }}">
                  {{ form_label(form.certificate, null, {'label_attr': {'class': 'col-form-label'}}) }}
                  {{ form_widget(form.certificate) }}
                </div>
              </div>
              <div class="row">
                <div class="col-12 {{ (form_errors(form.certificateChain) ? 'has-error' : '') }}">
                  {{ form_label(form.certificateChain, null, {'label_attr': {'class': 'col-form-label'}}) }}
                  {{ form_widget(form.certificateChain) }}
                </div>
              </div>
              <div class="row">
                <div class="col text-end">
                  {{ form_widget(form.submit) }}
                </div>
              </div>
            </div>
          </div>
        </div>
        {{ form_end(form) }}
      </div>
    </div>
  </div>
</div>
{% endblock %}