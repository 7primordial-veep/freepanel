{% if flashBagMessages %}
  {% for label, flashes in flashBagMessages %}
    <div class="alert-container">
      {% for flash in flashes %}
        <div class="alert alert-{{ label }}">
        {{ flash }}
      </div>
    {% endfor %}
  </div>
{% endfor %}
{% endif %}